package project.pj25.data;

/**
 * Zapis (Record) za čuvanje ukupnog broja prodatih karata i prihoda.
 */
public record SalesData(int totalTickets, double totalRevenue) {
    // Tijelo je prazno jer se automatski generiše
}